from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This product is being developed to serve users one stop solution for all their NLP needs',
    author='DS_NLP_CORE',
    license='MIT',
)
